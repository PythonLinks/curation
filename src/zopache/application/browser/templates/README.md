
I use pug in the browser to generate the html for this, then I use menu/fix to access the python.  And item.getHTML() to get the HTML. 

The problem is that \r\n needs to be replaced with a carriage return and 
\' needs to be replaced with just a single quote '
